server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://capstone.cs6kilq1qdog.us-west-1.rds.amazonaws.com:3306/capstone
    username: admin
    password: capstone
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        #        show_sql: true
        format_sql: true
        default_batch_fetch_size: 1000
#    open-in-view: false

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace
